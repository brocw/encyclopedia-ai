You are creating a Wikipedia-style infobox for the topic "%s". Based on the article below, extract key facts such as type/classification, dates, locations, notable attributes, and other essential summary data.

Respond with ONLY a JSON object in this exact format:
{"rows": [{"field": "Field Name", "value": "Field Value"}, ...]}

Example:
{"rows": [{"field": "Born", "value": "1879"}, {"field": "Nationality", "value": "German"}]}

Do not include any text outside the JSON object.

Article:
%s
